apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-pod-watcher.fullname" . }}-test-connection"
  labels:
    {{- include "chart-pod-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-pod-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
