apiVersion: v1
kind: Service
metadata:
  name: {{ include "anomaly-detection-stack.fullname" . }}
  labels:
    {{- include "anomaly-detection-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anomaly-detection-stack.selectorLabels" . | nindent 4 }}
